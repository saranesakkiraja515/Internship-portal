signupForm.addEventListener('submit', (e) => {
    e.preventDefault();

    // Example form validation or API submission logic

    // Redirect to success page
    window.location.href = 'success.html';
});
